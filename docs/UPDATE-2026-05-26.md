# UPDATE-2026-05-26 — Cleanup Laravel App build commands

## Tóm tắt

Đã xóa thư mục legacy `app/Console/Commands/App/` khỏi source chính.

Thư mục này chứa Laravel command `app:build` và các stub shell liên quan đến build/package app cũ:

- `BuildCommand.php`
- `stubs/build.sh`
- `stubs/dev.sh`
- `stubs/platform_valid.sh`
- `stubs/runkit.sh`

## Lý do

Dự án hiện tập trung vào Python CLI trong `src/ductn.py`, module `src/utils/`, legacy Bash trong `src/var/lib/`, và Debian packaging trong `src/debian/`.

Nhóm command `app:build` / `app:package` thuộc luồng Laravel app build cũ, không còn là path vận hành chính của package `ductn`.

## Ảnh hưởng

- Không còn source cho Laravel command `app:build` trong `app/Console/Commands/App/`.
- Không còn các stub shell build Laravel app nằm dưới thư mục đó.
- Build/package hiện hành cần dùng luồng Debian/Python documented trong `README.md` và `BOOTSTRAP.md`.

## Hướng dẫn cho agent/dev sau

- Không reference lại `app/Console/Commands/App/` như một path active.
- Nếu cần build package, dùng `cd src && ./build.sh` hoặc workflow Debian trong `src/debian/`.
- Nếu cần khôi phục logic Laravel app build cũ, tra lại Git history thay vì tạo lại mặc định.
