# UPDATE-2026-05-15 - Project documentation refresh

## Mục đích

Bổ sung tài liệu tổng quan cho dự án `ductn` trong bối cảnh dự án đang migrate từ Bash legacy sang Python CLI.

## Thay đổi

- Viết lại `README.md` để mô tả rõ mục đích package, cách cài đặt, cách chạy khi phát triển và kiến trúc thư mục.
- Ghi rõ trạng thái hiện tại: Bash scripts cũ vẫn nằm trong `src/var/lib/`, Python CLI mới nằm ở `src/ductn.py` và `src/utils/`.
- Bổ sung bảng nhóm command Python đã có để dễ kiểm tra phạm vi migrate.
- Bổ sung quy trình đề xuất khi migrate từng Bash script sang Python.
- Bổ sung phần build Debian package, dependencies, development rules và troubleshooting.

## Quyết định tài liệu

- `TASKS.md` tiếp tục là nơi tracking migration chi tiết.
- `README.md` đóng vai trò tài liệu onboarding/tổng quan cho agent hoặc developer mới.
- Chưa cập nhật số lượng migration trong `TASKS.md` vì task này chỉ viết tài liệu, không xác minh lại đầy đủ từng command Bash legacy.

## Lưu ý kỹ thuật phát hiện khi rà soát

- Import Python CLI trong sandbox root có thể lỗi vì logging ghi vào `/var/log/ductnd/ductnd.log` trên filesystem read-only.
- `src/utils/__init__.py` đang import `system_metrics`, nhưng file này không xuất hiện trong danh sách file ở lần rà soát hiện tại. Cần kiểm tra riêng trước khi chạy test CLI đầy đủ.
