# Daily Notes - 2026-05-15

## Session
- Đã đọc `SOUL.md`, `USER.md`, `IDENTITY.md` theo startup protocol.
- `MEMORY.md`, `TOOL.md` chưa tồn tại; `memory/2026-05-15.md` được tạo mới.
- Sếp yêu cầu báo cáo thông tin dự án `ductnd`.

## Project scan
- Repo hiện ở branch `main`, tracking `origin/main`, working tree sạch tại thời điểm kiểm tra.
- Remote: `https://github.com/diepxuan/management.git`.
- Package chính: `ductn`, Debian source package `diepxuan`, version hiện tại trong changelog `5.5.0+ubuntu~24.04`.
- Entry point Python CLI: `src/ductn.py`; command modules nằm trong `src/utils/`.
- Build/package metadata chính: `src/debian/control`, `src/debian/changelog`, `src/debian/rules`.

## Documentation update
- Sếp yêu cầu viết tài liệu dự án vì dự án cũ dùng Bash và đang migrate sang Python, còn nhiều chức năng chưa dùng/chưa migrate.
- Đã viết lại `README.md` theo hướng onboarding + migration guide.
- Đã tạo `docs/UPDATE-2026-05-15.md` ghi thay đổi tài liệu và lưu ý kỹ thuật.
- Phát hiện khi chạy import CLI trong sandbox root: lỗi ghi log `/var/log/ductnd/ductnd.log` read-only.
- Phát hiện `src/utils/__init__.py` import `system_metrics` nhưng chưa thấy file tương ứng trong repo scan.
